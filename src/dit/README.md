# Diffusion Transformer backbone

Planned owner of patchify/unpatchify, Transformer blocks, timestep/condition modulation and the mini-DiT prediction model.

It replaces the U-Net backbone but reuses diffusion objectives and samplers through stable interfaces. Attention must be implemented from basic PyTorch operations under the project scratch rules. Implementation begins only at A8.
