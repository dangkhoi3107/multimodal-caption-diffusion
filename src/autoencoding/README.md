# Autoencoding

Planned owner of convolutional encoder/decoder, VAE reparameterization, reconstruction/KL losses and latent statistics.

This package creates the learned spatial representation used by A7 Latent Diffusion. It does not own diffusion schedules, samplers or text conditioning. Implementation begins only at A6.
