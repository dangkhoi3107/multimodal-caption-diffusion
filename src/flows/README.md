# Generative flows

Planned owner of probability paths, velocity targets, Flow Matching objectives and ODE solvers used by A10.

Flow-specific names and equations stay separate from DDPM beta schedules. The first implementation is a 2D toy vertical slice; image/latent flow is added only after its path and solver tests pass.
