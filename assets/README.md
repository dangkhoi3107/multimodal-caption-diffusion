# Assets

Small, curated diagrams and figures used by the root README or final report belong here. Large training outputs and generated sample sets belong under `outputs/<phase>/<run_id>/` and should not be committed by default.
